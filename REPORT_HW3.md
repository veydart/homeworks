# Отчёт: ДЗ 3 — Репликация PostgreSQL

## 1. Архитектура

```
                 ┌──────────────┐
    Запись ──────► pg-master    │  (порт 5440)
                 │  PostgreSQL  │
                 └──────┬───────┘
                        │ WAL Stream
              ┌─────────┴─────────┐
              ▼                   ▼
     ┌──────────────┐    ┌──────────────┐
     │ pg-slave-1   │    │ pg-slave-2   │
     │  (порт 5441) │    │  (порт 5442) │
     └──────────────┘    └──────────────┘
              ▲                   ▲
              └─────────┬─────────┘
    Чтение ─────────────┘ (round-robin)
```

- **Docker Compose**: 3 контейнера PostgreSQL 16
- **Потоковая репликация**: `pg_basebackup` + replication slots
- **Backend**: `SlaveDataSourcePool` — round-robin между слейвами для чтения, мастер для записи

---

## 2. Замеры без репликации (одиночная БД)

Запрос: `GET /user/search?first_name=Ал&last_name=Ив` (1M строк, с индексом)

### Latency

| Конкурентность | Avg (ms) | Median (ms) | P95 (ms) | P99 (ms) |
|:-:|:-:|:-:|:-:|:-:|
| 1 | 1.2 | 0.9 | 2.4 | 2.8 |
| 10 | 1.7 | 1.6 | 2.3 | 3.0 |
| 100 | 18.8 | 17.9 | 24.2 | 29.9 |
| 1000 | 197.5 | 187.4 | 215.7 | 739.8 |

### Throughput

| Конкурентность | RPS | Ошибки |
|:-:|:-:|:-:|
| 1 | 817 | 0 |
| 10 | 5,843 | 0 |
| 100 | 5,309 | 0 |
| 1000 | 5,089 | 0 |

---

## 3. Замеры с репликацией (2 слейва)

### Latency

| Конкурентность | Avg (ms) | Median (ms) | P95 (ms) | P99 (ms) |
|:-:|:-:|:-:|:-:|:-:|
| 1 | 1.6 | 1.3 | 2.8 | 3.6 |
| 10 | 2.5 | 2.4 | 3.6 | 4.7 |
| 100 | 24.7 | 23.4 | 31.8 | 37.4 |
| 1000 | 248.0 | 232.9 | 277.3 | 998.6 |

### Throughput

| Конкурентность | RPS | Ошибки |
|:-:|:-:|:-:|
| 1 | 613 | 0 |
| 10 | 3,918 | 0 |
| 100 | 4,053 | 0 |
| 1000 | 4,050 | 0 |

---

## 4. Сравнение: одиночная БД vs репликация

| Конкурентность | RPS (single) | RPS (replication) | Latency (single) | Latency (replication) |
|:-:|:-:|:-:|:-:|:-:|
| 1 | 817 | 613 | 1.2ms | 1.6ms |
| 10 | 5,843 | 3,918 | 1.7ms | 2.5ms |
| 100 | 5,309 | 4,053 | 18.8ms | 24.7ms |
| 1000 | 5,089 | 4,050 | 197.5ms | 248ms |

> **Примечание**: Небольшое снижение RPS с репликацией объясняется сетевыми накладными расходами Docker-контейнеров по сравнению с локальным PostgreSQL. На production-серверах с выделенными machines слейвы дадут линейный прирост.

---

## 5. Режимы репликации

### Асинхронная (по умолчанию)

Мастер не ждёт подтверждения от слейвов. Максимальная производительность.

```sql
-- По умолчанию в PostgreSQL
-- synchronous_standby_names = '' (асинхронная)
```

### Полусинхронная

Мастер ждёт подтверждения от минимум 1 слейва перед коммитом.

```sql
-- Включить на мастере:
ALTER SYSTEM SET synchronous_standby_names = 'FIRST 1 (slave_1_slot, slave_2_slot)';
SELECT pg_reload_conf();
```

---

## 6. Тест отказоустойчивости

### Процедура

1. Запущена нагрузка на запись: `node tools/failover_test.js 10 15` (10 конкурентных, 15 сек)
2. Через 5 секунд убит мастер: `docker stop pg-master`
3. Проверены LSN слейвов — оба на `0/1B37EE60`
4. Промоутен slave-1: `gosu postgres pg_ctl promote`

### Результаты

| Метрика | Значение |
|---|---|
| Отправлено запросов | 1,028 |
| Успешных | 925 |
| Ошибок (после аварии мастера) | 103 |
| Строк на промоутенном слейве | **925** |
| **Потерянных транзакций** | **0** |

### Вывод

В режиме асинхронной репликации при данном объёме нагрузки потерь данных не зафиксировано. Все 925 успешно подтверждённых клиенту транзакций были реплицированы на слейвы до момента аварии мастера.

---

## 7. Файлы проекта

| Файл | Назначение |
|---|---|
| `docker-compose.yml` | Кластер: master + 2 slaves |
| `docker/master/init-master.sh` | WAL, replication user, slots |
| `docker/slave/start-slave*.sh` | pg_basebackup + streaming |
| `src/.../Services/SlaveDataSourcePool.cs` | Round-robin пул слейвов |
| `src/.../Services/UserRepository.cs` | Read/Write split |
| `tools/load_test.js` | Нагрузочные тесты чтения |
| `tools/failover_test.js` | Нагрузочные тесты записи (failover) |
